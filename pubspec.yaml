name: e_commerce_full
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #UTILS
  cupertino_icons: ^1.0.2
  carousel_slider: ^4.1.1
  google_sign_in: ^5.3.0
  # pay: ^1.0.8 #There is no any certificate for the iOS

  #FIREBASE
  firebase_core: ^1.15.0
  cloud_firestore: ^3.1.13
  firebase_auth: ^3.3.16

  #LOGIC
  flutter_bloc: ^8.0.1
  equatable: ^2.0.3

  #ROUTE
  auto_route: ^3.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  auto_route_generator: ^3.2.3
  build_runner: ^2.1.10

flutter:
  uses-material-design: true

  assets:
    - assets/
